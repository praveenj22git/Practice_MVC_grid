@model IEnumerable<MVC_PRAC_1.Models.ProductsIvy>
@using GridMvc.Html

@{
    ViewBag.Title = "DisplayProduct";
}

<h2>DisplayProduct</h2>

@*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product_Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product_Manufacturing_date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product_Expiry_date)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Product_Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Product_Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Product_Manufacturing_date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Product_Expiry_date)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Product_Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Product_Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Product_Id })
                </td>
            </tr>
        }

    </table>*@
<div class="row">
    <div class="col-md-12">
        @Html.Grid(Model).Named("ProductGrid").Columns(c =>
     {
       c.Add(x => x.Product_Id).Titled("Product Id").Filterable(true).Sortable(true);
         c.Add(x => x.Product_Name).Titled("Product Name").Filterable(true).Sortable(true);
         c.Add(x => x.Product_Price).Titled("Product Price").Filterable(true).Sortable(true);
         c.Add(x => x.Product_Manufacturing_date).Titled("Product Manufacturing date").Filterable(true).Sortable(true);
         c.Add(x => x.Product_Expiry_date).Titled("Product Expiry date").Filterable(true).Sortable(true);
     }).WithPaging(5).EmptyText("No Data Found")
    </div>
</div>



